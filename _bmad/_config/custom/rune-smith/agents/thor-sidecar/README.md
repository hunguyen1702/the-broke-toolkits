# Thor Sidecar

This folder holds persistent context and memory for the Thor agent when `hasSidecar: true`.

After module installation, user-specific overrides and quest context can be stored here per project (e.g. via `_bmad/_memory/thor-sidecar/` in the installed project).
