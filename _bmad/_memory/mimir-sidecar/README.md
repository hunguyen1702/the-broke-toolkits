# Mimir Sidecar

This folder holds persistent context and memory for the Mimir agent when `hasSidecar: true`.

After module installation, user-specific overrides and saga context can be stored here per project (e.g. via `_bmad/_memory/mimir-sidecar/` in the installed project).
